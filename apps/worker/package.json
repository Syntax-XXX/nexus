{
  "name": "@nexus/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "build": "tsc -p tsconfig.json", "dev": "tsx watch src/main.ts", "start": "node dist/main.js", "typecheck": "tsc -p tsconfig.json --noEmit", "lint": "eslint src", "clean": "rm -rf dist" },
  "dependencies": {
    "@nexus/config": "file:../../packages/config",
    "@nexus/database": "file:../../packages/database",
    "@nexus/logger": "file:../../packages/logger",
    "pg-boss": "12.28.0",
    "zod": "4.4.3"
  }
}
